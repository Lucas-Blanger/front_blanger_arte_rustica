services:
  frontend:
    build:
      context: .
    container_name: frontend-arte-rustica
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_API_URL: ${VITE_API_URL}
